<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beautysalon.mapper.PosCouponMapper">

    <!-- 分页查询优惠券 -->
    <select id="selectPage" resultType="com.beautysalon.entity.PosCoupon">
        SELECT * FROM pos_coupon
        WHERE deleted = 0
        <if test="campaignId != null">
            AND campaign_id = #{campaignId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 根据会员ID查询 -->
    <select id="selectByMemberId" resultType="com.beautysalon.entity.PosCoupon">
        SELECT * FROM pos_coupon
        WHERE deleted = 0 AND member_id = #{memberId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据会员ID和状态查询 -->
    <select id="selectByMemberIdAndStatus" resultType="com.beautysalon.entity.PosCoupon">
        SELECT * FROM pos_coupon
        WHERE deleted = 0 AND member_id = #{memberId} AND status = #{status}
        ORDER BY create_time DESC
    </select>

    <!-- 查询会员可用优惠券 -->
    <select id="selectAvailableByMemberId" resultType="com.beautysalon.entity.PosCoupon">
        SELECT * FROM pos_coupon
        WHERE deleted = 0 AND member_id = #{memberId}
          AND status = 1
          AND (end_date IS NULL OR end_date &gt;= #{now})
        ORDER BY create_time DESC
    </select>

    <!-- 根据活动ID查询 -->
    <select id="selectByCampaignId" resultType="com.beautysalon.entity.PosCoupon">
        SELECT * FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
        ORDER BY create_time DESC
    </select>

    <!-- 核销优惠券 -->
    <update id="useCoupon">
        UPDATE pos_coupon
        SET status = 2, update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 按编码核销 -->
    <update id="useCouponByCode">
        UPDATE pos_coupon
        SET status = 2, update_time = NOW()
        WHERE code = #{code} AND deleted = 0
    </update>

    <!-- 批量更新过期状态 -->
    <update id="batchUpdateExpiredStatus">
        UPDATE pos_coupon
        SET status = 3, update_time = #{now}
        WHERE deleted = 0 AND status = 1
          AND end_date &lt; #{now}
    </update>

    <!-- 统计已使用 -->
    <select id="countUsedByMemberId" resultType="int">
        SELECT COUNT(*) FROM pos_coupon
        WHERE deleted = 0 AND member_id = #{memberId} AND status = 2
    </select>

    <!-- 统计活动不同会员数 -->
    <select id="countDistinctMembersByCampaignId" resultType="int">
        SELECT COUNT(DISTINCT member_id) FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
    </select>

    <!-- 统计 -->
    <select id="countByCampaignId" resultType="int">
        SELECT COUNT(*) FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
    </select>

    <!-- 统计已使用 -->
    <select id="countUsedByCampaignId" resultType="int">
        SELECT COUNT(*) FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId} AND status = 2
    </select>

    <!-- 统计已过期 -->
    <select id="countExpiredByCampaignId" resultType="int">
        SELECT COUNT(*) FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId} AND status = 3
    </select>

    <!-- 统计 -->
    <select id="countByCampaignIdAndStatus" resultType="int">
        SELECT COUNT(*) FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <!-- 活动统计 -->
    <select id="selectCampaignStatistics" resultType="map">
        SELECT
            COUNT(*) AS total,
            SUM(CASE WHEN status = 1 THEN 1 ELSE 0 END) AS pending,
            SUM(CASE WHEN status = 2 THEN 1 ELSE 0 END) AS used,
            SUM(CASE WHEN status = 3 THEN 1 ELSE 0 END) AS expired
        FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
    </select>

    <!-- 活动每日统计 -->
    <select id="selectDailyStatisticsByCampaignId" resultType="map">
        SELECT
            DATE(create_time) AS date,
            COUNT(*) AS total,
            SUM(CASE WHEN status = 2 THEN 1 ELSE 0 END) AS used
        FROM pos_coupon
        WHERE deleted = 0 AND campaign_id = #{campaignId}
          AND create_time &gt;= #{startDate}
          AND create_time &lt;= #{endDate}
        GROUP BY DATE(create_time)
        ORDER BY date ASC
    </select>

</mapper>
